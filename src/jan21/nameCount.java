/*
package jan21;

import java.util.ArrayList;
import java.util.List;

public class nameCount {
    public static void main(String[] args) {
        List<String> nameList = new ArrayList<>();
        nameList.add("홍길동");
        nameList.add("임꺽정");

    }

    //dooli
    public nameCount(List<String> nameList) {
        String currentText = ""; //d
        int  currentTextCount = 0; //1
        String savedText = ""; //d
        int savedTextCount =0; //1

        for(int i =0; i<nameList.size(); i++){
            currentText = nameList.get(i);
            if(i ==0){
                currentText = nameList.get(0);
                savedText = currentText;
                currentTextCount = 1;
                savedTextCount = currentTextCount;
            }
            for(int j=0; j<nameList.size()-1; j++){
                if(nameList.get(i).equals(nameList.get(j))){
                    savedTextCount++;
                    currentText = nameList.get(j);
                }
            }
            if()
        }



    }
}


*/
